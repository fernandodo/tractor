`c` 命令 = 删除 + 自动进入插入模式，一步完成编辑

## 1. 基础修改命令

| 命令   | 范围                | 说明                | 示例                                                                                   |
| ---- | ----------------- | ----------------- | ------------------------------------------------------------------------------------ |
| `cc` | 修改整行（保持缩进）        | 删除整行内容并进入插入模式     | `    int x = 0;` 按 `cc` 输入 `float y = 1.5;` → `    float y = 1.5;`                   |
| `cw` | 光标 → 单词结尾         | 从当前位置到单词尾部        | `int value;` 光标在 `v` 上，按 `cw` 输入 `count` → `int count;`                              |
| `ce` | 光标 → 单词结尾（包含结尾字符） | 类似 `cw`，但保证吃掉单词末尾 | `foo bar` 光标在 `f`，按 `ce` 输入 `hello` → `hello bar`                                    |
| `c$` | 光标 → 行尾           | 改写光标到行尾的所有内容      | `printf("Hello world");` 光标在 `w` 上，按 `c$` 输入 `VSCode");` → `printf("Hello VSCode");` |
| `c0` | 行首 → 光标           | 改写从行首到当前位置        | `int value = 10;` 光标在 `=`，按 `c0` 输入 `float x ` → `float x = 10;`                     |

## 2. 文本对象修改命令

| 命令    | 范围               | 说明                | 示例                                                                                 |
| ----- | ---------------- | ----------------- | ---------------------------------------------------------------------------------- |
| `ciw` | 当前单词（inner word） | 无论光标在单词哪儿，都改整个单词  | `int value;` 光标在 `value` 任意位置，按 `ciw` 输入 `count` → `int count;`                    |
| `caw` | 当前单词 + 周围空格      | 连同前后空格一起改         | `int value = 10;` 光标在 `value`，按 `caw` 输入 `x` → `int x= 10;`                        |
| `ci"` | 引号内文本            | 改写引号内内容           | `char *msg = "old text";` 光标在引号内，按 `ci"` 输入 `new text` → `char *msg = "new text";` |
| `ci'` | 单引号内文本           | 改写单引号内内容          | `char c = 'a';` 光标在 `a`，按 `ci'` 输入 `b` → `char c = 'b';`                           |
| `ci(` | 小括号内文本           | 改写小括号内容           | `printf("Hello");` 光标在 `Hello` 内，按 `ci(` 输入 `"World"` → `printf("World");`         |
| `ci[` | 方括号内文本           | 改写方括号内容           | `arr[index]` 光标在 `index`，按 `ci[` 输入 `0` → `arr[0]`                                 |
| `ci{` | 花括号内文本           | 改写花括号内容           | `if(x){statement;}` 光标在大括号内，按 `ci{` → 清空大括号内容并插入                                   |
| `ci}` | 花括号内文本           | 和 `ci{` 等价        | 同上                                                                                 |
| `cit` | 标签内文本            | 改写 HTML/XML 标签内内容 | `<div>hello</div>` 光标在 `hello`，按 `cit` 输入 `world` → `<div>world</div>`             |

## 3. 特殊字符查找修改

| 命令     | 范围            | 说明             | 示例                                                     |
| ------ | ------------- | -------------- | ------------------------------------------------------ |
| `ci_`  | 下划线之间的内容      | 改写下划线包围的内容     | `fv_abc_def` 光标在 `abc`，按 `ci_` 输入 `xyz` → `fv_xyz_def` |
| `ct_`  | 修改到下划线为止      | 改写到下一个下划线      | `fv_abc_def` 光标在开头，按 `ct_` 输入 `new` → `new_abc_def`    |
| `cT_`  | 修改到上个下划线为止    | 改写到上一个下划线      | `fv_abc_def` 光标在 `def`，按 `cT_` 输入 `xyz` → `fv_abc_xyz` |
| `cf字符` | 修改到指定字符（包含）   | 改写到指定字符（包含该字符） | `hello world` 光标在 `h`，按 `cfw` 输入 `hi ` → `hi world`    |
| `cF字符` | 修改到指定字符（向前查找） | 向前查找字符并改写      | `hello world` 光标在 `w`，按 `cFl` 输入 `x` → `hexorld`       |

## 4. 段落和大范围修改

| 命令    | 范围                  | 说明         | 示例                          |
| ----- | ------------------- | ---------- | --------------------------- |
| `cip` | 段落（inner paragraph） | 改写光标所在整个段落 | 光标在一段注释中，`cip` → 清空段落并插入新内容 |
| `cap` | 段落 + 周围空行           | 改写段落包括前后空行 | 同上，但包括段落前后的空行               |
| `cG`  | 光标 → 文件末尾           | 改写到文件尾     | 删除当前位置到文件结尾所有内容并插入          |
| `cgg` | 光标 → 文件开头           | 改写到文件头     | 删除当前位置到文件开头所有内容并插入          |
| `c}`  | 光标 → 下一个段落          | 改写到下个空行    | 删除到下个段落分隔符并插入               |
| `c{`  | 光标 → 上一个段落          | 改写到上个空行    | 删除到上个段落分隔符并插入               |

## 5. 数字组合修改

|命令|范围|说明|示例|
|---|---|---|---|
|`c3w`|修改 3 个单词|删除接下来 3 个单词并插入|`one two three four` 光标在 `o`，按 `c3w` → 删除 `one two three` 并插入|
|`c2l`|修改右边 2 个字符|精确改写几个字符|`abcdef` 光标在 `a`，按 `c2l` 输入 `XY` → `XYcdef`|
|`c5j`|修改 5 行（向下）|改写当前行及下面 4 行|删除 5 行内容并插入|

## 核心记忆法

- `c` = **change = delete + insert**
- `c{motion}` → 改写某个移动范围
- `ci{object}` → 改写文本对象内部（inner）
- `ca{object}` → 改写文本对象及其边界（around）

## 使用场景总结

- **小改动** → `cw`、`c$`、`c2l`
- **整个单词** → `ciw`、`caw`
- **括号/引号内容** → `ci(`、`ci{`、`ci"`
- **大段落/函数** → `cip`、`c}`、`cG`
- **查找替换** → `ct字符`、`cf字符`

## 组合总结

| 对象/范围           | `c` (only)                                                       | `ci` (change inner)                   | `ca` (change around)                      | `ct` / `cT`                                                                             | `cf` / `cF`                                                                                     |
| --------------- | ---------------------------------------------------------------- | ------------------------------------- | ----------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **单词**          | `cw` / `ce` → 改写到单词结尾                                            | `ciw` → 改整个单词                         | `caw` → 连空格一起改                            | –                                                                                       | –                                                                                               |
| **段落**          | `c}` / `c{` → 改写到段落结束/开始                                         | `cip` → 改写整段落                         | `cap` → 改写段落 + 空行                         | –                                                                                       | –                                                                                               |
| **行**           | `c$` → 改写到行尾<br/>`c0` → 改写到行首<br/>`c^` → 改写到行首非空<br/>`cc` → 改写整行 | –                                     | –                                         | –                                                                                       | –                                                                                               |
| **文件**          | `cG` → 改写到文件尾<br/>`cgg` → 改写到文件头                                 | –                                     | –                                         | –                                                                                       | –                                                                                               |
| **引号 " '**      | –                                                                | `ci"` → 改写双引号内文本<br/>`ci'` → 改写单引号内文本 | `ca"` → 改写双引号及引号本身<br/>`ca'` → 改写单引号及引号本身 | `ct"` → 改写到下一个双引号<br/>`ct'` → 改写到下一个单引号<br/>`cT"` → 改写到上一个双引号<br/>`cT'` → 改写到上一个单引号     | `cf"` → 改写到并含下一个双引号<br/>`cf'` → 改写到并含下一个单引号<br/>`cF"` → 改写到并含上一个双引号<br/>`cF'` → 改写到并含上一个单引号     |
| **小括号 ()**      | `c)` / `c(` → 改写到句子结束/开始                                         | `ci(` / `ci)` → 改写括号内文本               | `ca(` / `ca)` → 包括括号一起改                   | `ct(` → 改写到下一个左括号<br/>`ct)` → 改写到下一个右括号<br/>`cT(` → 改写到上一个左括号<br/>`cT)` → 改写到上一个右括号     | `cf(` → 改写到并含下一个左括号<br/>`cf)` → 改写到并含下一个右括号<br/>`cF(` → 改写到并含上一个左括号<br/>`cF)` → 改写到并含上一个右括号     |
| **大括号 {}**      | –                                                                | `ci{` / `ci}` → 改写大括号内文本              | `ca{` / `ca}` → 包括大括号一起改                  | `ct{` → 改写到下一个左大括号<br/>`ct}` → 改写到下一个右大括号<br/>`cT{` → 改写到上一个左大括号<br/>`cT}` → 改写到上一个右大括号 | `cf{` → 改写到并含下一个左大括号<br/>`cf}` → 改写到并含下一个右大括号<br/>`cF{` → 改写到并含上一个左大括号<br/>`cF}` → 改写到并含上一个右大括号 |
| **方括号 []**      | –                                                                | `ci[` / `ci]` → 改写方括号内文本              | `ca[` / `ca]` → 包括方括号一起改                  | `ct[` → 改写到下一个左方括号<br/>`ct]` → 改写到下一个右方括号<br/>`cT[` → 改写到上一个左方括号<br/>`cT]` → 改写到上一个右方括号 | `cf[` → 改写到并含下一个左方括号<br/>`cf]` → 改写到并含下一个右方括号<br/>`cF[` → 改写到并含上一个左方括号<br/>`cF]` → 改写到并含上一个右方括号 |
| **尖括号 <>**      | –                                                                | `ci<` / `ci>` → 改写尖括号内文本              | `ca<` / `ca>` → 包括尖括号一起改                  | `ct<` → 改写到下一个左尖括号<br/>`ct>` → 改写到下一个右尖括号<br/>`cT<` → 改写到上一个左尖括号<br/>`cT>` → 改写到上一个右尖括号 | `cf<` → 改写到并含下一个左尖括号<br/>`cf>` → 改写到并含下一个右尖括号<br/>`cF<` → 改写到并含上一个左尖括号<br/>`cF>` → 改写到并含上一个右尖括号 |
| **HTML/XML 标签** | –                                                                | `cit` → 改写标签内文本                       | `cat` → 包括标签一起改                           | –                                                                                       | –                                                                                               |
| **下划线/特殊字符**    | –                                                                | `ci_` → 改写下划线间内容                      | `ca_` → 包括下划线一起改                          | `ct_` → 改写到下一个下划线<br/>`cT_` → 改写到上一个下划线                                                 | `cf_` → 改写到并含下一个下划线<br/>`cF_` → 改写到并含上一个下划线                                                     |
| **任意字符**        | –                                                                | –                                     | –                                         | `ctx` → 改写直到下一个 `x`（不含）<br/>`cTx` → 改写直到上一个 `x`（不含）                                     | `cfx` → 改写直到并含下一个 `x`<br/>`cFx` → 改写直到并含上一个 `x`                                                 |
| **搜索模式**        | `c/pattern` → 改写到搜索结果<br/>`c?pattern` → 改写到反向搜索结果                | –                                     | –                                         | –                                                                                       | –                                                                                               |
| **数字组合**        | `c2w` → 改写2个单词<br/>`c3l` → 改写3个字符<br/>`c5j` → 改写5行向下             | –                                     | –                                         | –                                                                                       | –                                                                                               |
